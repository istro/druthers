= form_for [@discussion, @solution] do |f|
  = f.label :text, "Suggest your solution:"
  = f.text_field :text, class: 'text_field', placeholder: 'I\'d rather...'
  = f.submit 'Propose new solution', class: "btn btn-info"
